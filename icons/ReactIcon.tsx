import React from "react";

function ReactIcon() {
  return (
    <section className="w-full h-full p-2">
      <img src={"/react-js-icon.svg"} alt="" className={`w-full h-full `} />
    </section>
  );
}

export default ReactIcon;
